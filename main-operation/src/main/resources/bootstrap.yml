spring:
  cloud:
    config:
      name: main-operation #需要从github上读取的资源名称，注意没有yml后缀名
      profile: dev  #profile配置是什么就取什么配置dev or test
      discovery:
        enabled: true
        serviceId: config-server  #跳转项目的名字

eureka:
  client: #客户端注册进eureka服务列表内
    service-url:
       defaultZone: http://localhost:8091/eureka/